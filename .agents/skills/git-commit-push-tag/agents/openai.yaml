---
interface:
  display_name: "Git Commit Push Tag"
  short_description: >-
    Guarded commit, tag, push, and release flow.
  default_prompt: >-
    Use $git-commit-push-tag to analyze the next SemVer bump; mutate only with
    an explicit BUMP and create a requested stable GitHub Release without
    assets from the repository release-notes template.
policy:
  allow_implicit_invocation: false
